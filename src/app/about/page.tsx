import React from 'react';

import AboutIndex from '@/components/page/about/AboutIndex';

export type PageProps = {};

const Page = ({}: PageProps): React.ReactElement => <AboutIndex />;

export default Page;